[build]
  command = "pnpm install && pnpm --filter @workspace/mana-ai run build"
  publish = "artifacts/mana-ai/dist/public"

[build.environment]
  NODE_ENV = "production"
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
